# 配置

## 主配置文件

`plugins/DreamRealms/config.yml`

```yaml
# 物品名称语言
item-language: zh_cn

# 模块开关
modules:
  timesync: true
```

## 模块配置

每个模块有独立的配置文件，位于 `plugins/DreamRealms/modules/[模块名]/settings.yml`
